---
"@fluojs/cli": minor
---

Add module slice-test, resource slice-test, and e2e test generators so generated projects can scaffold the canonical fluo TDD ladder with `createTestingModule({ rootModule })` and `createTestApp({ rootModule })`.
