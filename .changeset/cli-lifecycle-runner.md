---
"@fluojs/cli": minor
---

Align generated project lifecycle scripts around `fluo dev` and `fluo start`, with CLI-owned runtime commands and Next.js-like `NODE_ENV` defaults that preserve explicitly provided environment values.
