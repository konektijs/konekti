{
  "name": "@konekti/example-ops-metrics-terminus",
  "description": "Runnable Konekti operations example with metrics and terminus health/readiness endpoints.",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@konekti/core": "workspace:*",
    "@konekti/http": "workspace:*",
    "@konekti/metrics": "workspace:*",
    "@konekti/runtime": "workspace:*",
    "@konekti/terminus": "workspace:*"
  },
  "devDependencies": {
    "@konekti/testing": "workspace:*"
  }
}
